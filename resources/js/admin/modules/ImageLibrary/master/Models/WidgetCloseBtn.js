
import BaseModel from './BaseModel.js';

const WidgetCloseBtn = (elem) => {

	const $this = elem || $('#media-library-widget-close-btn');

	BaseModel($this);

	// ==============================================================
	// Methods
	// ==============================================================

		

	// ==============================================================

	return $this;
};

export default WidgetCloseBtn;