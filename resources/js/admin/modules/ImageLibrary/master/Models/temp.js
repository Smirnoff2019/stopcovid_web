
import BaseModel from './BaseModel.js';

const Name = (elem) => {

	const $this = elem || $('selector');

	BaseModel($this);

	// ==============================================================
	// Methods
	// ==============================================================

		

	// ==============================================================

	return $this;
};

export default Name;